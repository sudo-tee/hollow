--- a/src/terminal/page.zig	2026-09-01 06:48:30.062414666 -0400
+++ b/src/terminal/page.zig	2026-09-03 07:29:21.677513841 -0400
@@ -1829,7 +1829,10 @@
 pub const std_capacity: Capacity = .{
     .cols = 215,
     .rows = 215,
-    .styles = 128,
+    // 128 is too small for style-heavy TUI output: a single 200x60 screen
+    // can hold 100+ live styles and exhaust the set during repaint churn.
+    // 512 provides bounded headroom above observed style-heavy workloads.
+    .styles = 512,
     .grapheme_bytes = if (builtin.is_test) 512 else 8192,
 };
 
